{
  "mcpServers": {
    "example-server": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem", "${CLAUDE_PLUGIN_ROOT}/data"],
      "description": "Example MCP server providing filesystem access",
      "env": {
        "EXAMPLE_CONFIG": "${CLAUDE_PLUGIN_ROOT}/config.json"
      }
    }
  }
}
